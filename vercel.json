{
    "functions": {
        "server/api/**/*.ts": {
            "runtime": "vercel-edge@2.5.0"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/server/api/$1"
        }
    ]
}